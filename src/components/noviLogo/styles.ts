import styled from "styled-components";

export const NoviLogoStyles = styled.h1`
    font-family: ${props => props.theme.fonts.fontAudiowide};
    font-size: 3rem;
    color: ${props => props.theme.colors.primaryColor};
`;